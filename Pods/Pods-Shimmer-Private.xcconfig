#include "Pods-Shimmer.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/Shimmer" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/BORChat" "${PODS_ROOT}/Headers/Shimmer"
OTHER_LDFLAGS = -ObjC
PODS_ROOT = ${SRCROOT}